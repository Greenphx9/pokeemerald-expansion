#ifdef __INTELLISENSE__
const struct SpeciesInfo gSpeciesInfoRebalanced[NUM_SPECIES] =
{
#endif

    // Add custom rebalanced stats here:
    // Currently supports base stats, abilities, types
    // Eg:
    
    /********************
    [SPECIES_BULBASAUR] = 
    {
        .baseHP = 255,
        .baseAttack = 255,
        .baseDefense = 255,
        .baseSpAttack = 255,
        .baseSpDefense = 255,
        .baseSpeed = 255,
        .abilities = { ABILITY_HUGE_POWER, ABILITY_NONE, ABILITY_PURE_POWER },
        .types = { TYPE_FIRE, TYPE_WATER },
    },
    ********************/

#ifdef __INTELLISENSE__
};
#endif
